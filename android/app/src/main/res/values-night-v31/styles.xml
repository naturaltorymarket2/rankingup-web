<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12(API 31) 이상 전용 테마.

  Android 12부터는 앱 실행 시 OS가 스플래시 화면을 강제로 표시하며,
  전체 이미지는 넣을 수 없고 '중앙 아이콘 + 배경색 + 하단 브랜딩 이미지'만
  지정할 수 있다. (구글 정책이라 전체 배너로 대체하는 것은 불가능)

  그래서 이렇게 구성한다.
    - 배경색   : 배너와 같은 톤 → 전환 시 색이 튀지 않는다
    - 아이콘   : 여백을 준 전용 아이콘(splash_icon)
                 런처 아이콘을 그대로 쓰면 원형 마스크에 잘려 확대돼 보인다
    - 브랜딩   : 하단에 서비스명 로고 노출 → 아이콘만 덩그러니 있는 느낌 제거
-->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="android:windowSplashScreenBrandingImage">@drawable/splash_branding</item>
        <!-- 엔진 준비 후 이어질 화면 (전체 배너) -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
</resources>
