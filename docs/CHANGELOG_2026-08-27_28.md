# 변경 내역 — 2026-08-27 ~ 08-28

> 맥 개발자 공유용 요약. 상세 배경은 `CLAUDE.md` 섹션 16(협업 규칙)·17(대규모 변경)을 참고.
> 저장소: `naturaltorymarket2/rankingup-web` (앱·웹·도구) / `naturaltorymarket2/rankingup` (랭킹 서버)

---

## 1. 어드민 (광고주/운영 웹 — Flutter Web)

| 항목 | 내용 | 관련 커밋 |
|------|------|-----------|
| 광고 삭제 기능 | `/admin/campaign` 하단을 '전체 광고 목록'으로 교체. 행마다 [삭제] — 미션 이력·순위 기록·태그까지 일괄 삭제. 삭제 확인창에 미션 건수와 '포인트 미환불' 경고 | `cd06807` |
| 신규 RPC | `delete_campaign_group`, `get_all_campaigns` (둘 다 ADMIN 검증) | migration 0043 |
| 어드민 로그인 | 이메일 → **아이디 방식**(`admin`)으로 변경. 내부적으로 `admin@quizcashnow.co.kr` 로 변환 | 08/27 |
| 로그아웃 버튼 | 어드민 4개 화면(충전/출금/공지/광고승인) AppBar 에 추가 — 계정 전환 수단이 없었음 | 08/27 |
| 어드민 role 검증 | 로그인 시 `users.role='ADMIN'` 확인 후 아니면 즉시 로그아웃 | 08/27 |

---

## 2. 광고주 웹

| 항목 | 내용 | 관련 커밋 |
|------|------|-----------|
| 배포 캐시 문제 | nginx 가 `main.dart.js` 를 1년 immutable 로 캐시해, 새로 배포해도 브라우저가 옛 화면을 계속 사용. 진입 파일 no-cache + 최초 1회 서비스워커/캐시 정리 | `77cad15` |
| 순위 차트 날짜 중복 | fl_chart 눈금 간격(interval) 미지정으로 소수점 위치까지 눈금이 생겨 같은 날짜가 20번 표시 | `f5c664d` |
| 순위 차트 y축 중복 | 위와 같은 원인으로 "41위, 41위" 중복 표기 | `1b05b41` |
| 당일 순위 미표시 | 크롤러가 대표 캠페인을 UUID 순으로 골라, 대시보드(=`created_at` 최소)와 불일치. 기준 통일 | `1b05b41` |
| 광고 상세 차트 | 1~15위 고정 스케일이라 30~40위대 상품은 차트가 비어 있었음 → 데이터 범위에 맞춰 동적 계산 | `f5c664d` |
| 등록 화면 | 키워드 추천 5개로 축소, 통합검색 10위 기준 표기, 상품명·업체명 서버 전달 | 08/27 |
| **광고 시작일** | **내일부터만 선택 가능**. 등록 당일은 순위·썸네일 크롤링 정보가 없어 유저가 상품을 찾을 단서가 없다 | `e331629` |

---

## 3. 앱 (Android)

| 항목 | 내용 | 버전 |
|------|------|------|
| **기기 식별 버그** | `androidInfo.id`(펌웨어 빌드번호)를 기기 고유값으로 사용 → 같은 기종 사용자가 서로 차단됨. **ANDROID_ID 로 교체** | 28 |
| 시작 화면 | 실행 즉시 브랜드 배너 표시(기존: 흰 배경 + 아이콘 4~5초). 세션 확인 대기 3초 → 1.2초 | 28 |
| 첫 실행 온보딩 | 3장 안내(검색 → 태그 확인 → 적립), 건너뛰기, 1회만 표시 | 28 |
| 앱 아이콘 | 844×824 비정사각 → 1024 정사각 + 적응형 아이콘(배경/전경 분리) | 28 |
| 상품 위치 힌트 | "약 37위 · 2페이지쯤에 있어요" + 상품명·판매자 표시 | 29 |
| 미션 보드 | 참여한 미션이 사라지지 않고 유지, 진행중 상태 구분, 필터 추가 | 29 |
| **상품 썸네일** | 미션 상세·태그 입력 화면에 상품 사진. 같은 판매자의 용량만 다른 상품과 혼동해 오답 처리되던 문제 대응 | 30 |
| 오답 입력 기록 | `mission_logs.attempt_count / last_submitted_tag` — 무엇을 입력했는지 확인 불가하던 문제 | 30 |
| Android 12+ 스플래시 | 전용 아이콘(여백 확보) + 하단 브랜딩 이미지 + 배경색을 배너 톤으로 | 30 |
| 출금 안내 | 잔액 5,000P 미만이면 입력 폼 대신 안내 화면 | 30 |
| **진행 중 미션 이어하기** | 앱을 나갔다 오면 재진입이 불가능하던 문제. 참여 내역에서 탭하면 태그 입력 화면 복귀 | 31 |
| 참여 내역 개선 | 상품 사진·상품명, 이번 달 요약(건수/적립), 실패 사유, `+7P` 표기 | 31 |
| 미션 보드 필터 | 전체 / 참여가능 / **진행중** / 참여완료 (진행중이 완료로 묶이던 문제) | 31 |
| 홈 카드 썸네일 | 목록에서 바로 상품 확인 | 31 |
| 미션 노출 조건 | `start_date` 가 지난 광고만 노출 (기존에는 시작일을 확인하지 않음) | 31 |

---

## 4. 서버 / 데이터 (랭킹 서버 · 크롤러 · DB)

| 항목 | 내용 |
|------|------|
| **네이버 공식 쇼핑 API 종료** | `openapi.naver.com/v1/search/shop.json` 이 404. 이 API 에 의존하던 순위 조회·일일 추적이 전부 실패 중이었음(마지막 정상 기록 2026-07-30) |
| 등록 시 순위 조회 | **SerpApi 네이버 통합검색**으로 대체. 광고 등록 1건당 5회 소모 (Starter 월 1,000회) |
| 조합 키워드 생성 | 상품명 토큰 + 시드 조합, 정보성 검색어(만들기/효능/복용법) 제외, 최대 5개 |
| 일일 순위 추적 | **로컬 크롤러**(`tools/reward_rank_crawler.py`)가 담당. 최대 500위, 미발견 시 `rank=NULL`(= '500위 밖') |
| 수집 주기 | 시드 키워드 매일 / 미션 키워드 7일 주기 + 1회 최대 20개 (광고주 증가 대비) |
| 썸네일 수집 | 크롤링 중 확보한 상품 이미지를 `campaigns.thumbnail_url` 에 저장 (같은 product_url 전체 반영) |
| 자동 실행 | Windows 작업 스케줄러 — 기존 `NaverRank_Daily`(09:00, 순위 모니터링)는 유지, 신규 `리워드 광고 순위 수집`(09:30) 분리. 크롬 디버그 포트 충돌 방지를 위해 포트 대기 로직 포함 |
| 스크립트 인코딩 | `.bat` 이 한글 경로를 CP949 로 잘못 읽어 무반응 종료 + `wmic` 제거 → PowerShell(.ps1, UTF-8 BOM) 전환 |
| Railway 스케줄러 | SerpApi 기반 일일 갱신 비활성화 (쿼터 보호) |

### 적용된 마이그레이션

| 번호 | 내용 | 상태 |
|------|------|------|
| 0043 | 어드민 광고 삭제, 순위 NULL 허용(500위 밖), `checked_to` | ✅ 08/27 |
| 0044 | `campaign_rank_history.keyword / is_seed` — 키워드별 순위 기록, 앱 조회 RLS | ✅ 08/28 |
| 0045 | `campaigns.thumbnail_url`, `mission_logs.attempt_count / last_submitted_tag`, `verify_mission` 갱신 | ✅ 08/28 |
| 0046 | `get_active_mission` RPC — 진행 중 미션 이어하기 (정답 태그는 미노출) | ✅ 08/28 |

> 합본 파일: `supabase/apply_phase23.sql` ~ `apply_phase26.sql`

---

## 5. 개발 환경

| 항목 | 내용 |
|------|------|
| 윈도우 PC 빌드 환경 | Android SDK 36 + JDK 17 + Flutter 3.47.1 구축 완료 → 맥 없이 AAB 빌드 가능 |
| 서명 키 | `C:/android/keystore/upload-keystore.jks` (저장소 밖). `.gitignore` 에 `*.jks`, `key.properties` 추가 |
| 배포 이력 | 27(미사용) → 28 · 29 · 30 출시 완료, 31 업로드 중 |

---

## 6. 남은 과제

| 항목 | 비고 |
|------|------|
| 미션 완주율 | 이어하기(31) 반영 후 효과 측정 필요. 이탈 지점 로깅은 미구현 |
| 정답 입력 방식 | 현재 정확 타이핑 필요 — 객관식 전환은 체류시간 확보와 함께 검토 예정 |
| SerpApi 쿼터 | Starter 월 1,000회. 광고주 증가 시 플랜 상향 필요 |
| 크롤러 PC 종속 | 로그인된 실제 크롬이 필요해 서버 이전 불가 (네이버 차단 정책) |
