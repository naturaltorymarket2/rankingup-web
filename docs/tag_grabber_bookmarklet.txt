javascript:(()=>{const s=new Set(),o=[];const push=t=>{t=(t||'').trim().replace(/^#+/,'');if(!t||t.length>30||/\s{2,}/.test(t))return;const k=t.replace(/\s/g,'').toLowerCase();if(k&&!s.has(k)){s.add(k);o.push(t);}};document.querySelectorAll('a,span,li,em,strong').forEach(el=>{if(el.children.length)return;const t=el.textContent||'';if(/^#\S/.test(t.trim()))push(t);});if(!o.length){(document.documentElement.innerHTML.match(/#[^\s"'<>#,]{1,30}/g)||[]).forEach(push);}const r=o.slice(0,10),out=r.map(t=>'#'+t).join('');if(!out){alert('태그를 찾지 못했습니다. 상품명 아래 태그가 보이는 곳까지 스크롤한 뒤 다시 눌러주세요.');return;}navigator.clipboard.writeText(out).then(()=>alert('태그 '+r.length+'개 복사됨

'+out),()=>prompt('아래 값을 복사하세요 (Ctrl+C)',out));})();